# MMA Platform Environment Configuration

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=admin
DATABASE_NAME=mma_platform

# Application Configuration
NODE_ENV=development
PORT=3000

# Docker Database Configuration (for docker-compose)
DB_USERNAME=postgres
DB_PASSWORD=admin
DB_NAME=mma_platform

# PgAdmin Configuration (optional)
PGADMIN_EMAIL=admin@mma-platform.com
PGADMIN_PASSWORD=admin

# Production Database URLs (for deployment)
# DATABASE_URL=postgresql://username:password@host:port/database

# JWT Configuration (if implementing authentication)
# JWT_SECRET=your-super-secret-jwt-key
# JWT_EXPIRES_IN=24h

# Redis Configuration (if using for caching)
# REDIS_HOST=redis
# REDIS_PORT=6379

# Logging Configuration
LOG_LEVEL=debug
